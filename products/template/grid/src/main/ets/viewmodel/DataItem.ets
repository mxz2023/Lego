/**
 * Grid item data.
 */
export class DataItem {
  /**
   * Grid item image.
   */
  image: Resource = $r('app.media.ic_grid_item');

  /**
   * Grid item title.
   */
  title: Resource = $r('app.string.grid_item_text');
}